# ═══════════════════════════════════════════════════════
# SFTP Secure Service — Environment Variables (EXAMPLE)
# ═══════════════════════════════════════════════════════
# Copy to .env and generate real secrets with:
#   ./scripts/init.sh --dev   (development)
#   ./scripts/init.sh --prod  (production)

# Application Secrets (generate with: openssl rand -hex 64)
SECRET_KEY=CHANGE_ME_TO_RANDOM_HEX
JWT_SECRET=CHANGE_ME_TO_RANDOM_HEX
JWT_EXPIRY_MINUTES=15

# LDAP
LDAP_ADMIN_PASSWORD=CHANGE_ME
LDAP_CONFIG_PASSWORD=CHANGE_ME

# Redis
REDIS_PASSWORD=CHANGE_ME

# SMTP (production: configure real SMTP relay)
SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_USER=
SMTP_PASSWORD=
SMTP_USE_TLS=false
SMTP_FROM=sftp-service@secure.local

# Ticket TTL (seconds)
TICKET_TTL_SECONDS=300

# File size limit (bytes) — 500MB
MAX_FILE_SIZE=524288000
